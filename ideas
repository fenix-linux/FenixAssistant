-The idea of Fenix Assitant is that the user does not have to type commands, so to do something just select the option in the menu.
-This includes the most used commands like apt get install, upgrade, purge, clean, autoremove, install -f..
-Add the script to allow the addition of repositories to Raspbian and MX Linux that fenixpi currently has.
-Htop
-App list
-Install drivers
-Install chromium docker
-Put the script I made June that downloads the icons and themes from the internet and put it in the wizard to avoid problems with copyright
 Themes and iconos: adapta, materialize, numix, papyrus, moka, paper, vimix, pop, layan,  orchis, aurora nuevo, Chrome os
-Script to switch from openbox to lxde for people who don't know that there are more desktops.
-Script to overclock Swiftjaro and choose different profiles without exceeding the limit of the warranty.
-grep, find
-Abrir programas autoarrsnque
-Xrandr
-history
-dd
-wget fenixlinux
-netstat
-ssh connect
-ftp connect
-scp
-rsync
-gpg
-rsync
-du,df
-Create versions with only one desktop and then the user chooses if he wants to install more desktop environments with taskel, apt-get or desktopify and then ask if he wants to download my themes from the internet. That way the distros are lighter.
-Integrar Mycroft Assitant.
https://github.com/MertcanGokgoz/UsefulBashScripts/tree/master/Scripts
https://github.com/davidhcefx/My-Bash-Scripts
https://github.com/adi1090x/dynamic-wallpaper
ping
setxkbmap en,es...
update system: # System
sudo apt update && apt full-upgrade

sudo apt autoclean && apt autoremove

snap refresh

flatpak update
# Ruby
gem update gem outdated | cut -d ' ' -f 1

# Python
pip install --upgrade pip

pip3 freeze --local | grep -v '^-e' | cut -d = -f 1 | xargs -n1 pip install -U

# Javascript
npm update -g

yarn upgrade

# Trash
trash-empty

# Dependencies
sudo deborphan | xargs sudo apt-get -y remove --purge

sudo deborphan --guess-data | xargs sudo apt-get -y remove --purge

# Cache
sudo sh -c 'echo 1 >/proc/sys/vm/drop_caches'
rm -rf /home//.local/share/Trash// &> /dev/null
rm -rf /root/.local/share/Trash/*/ &> /dev/null
# Remove apt / apt-get files
sudo apt clean
sudo apt -s clean
sudo apt clean all
sudo apt autoremove
sudo apt-get clean
sudo apt-get -s clean
sudo apt-get clean all
sudo apt-get autoclean

#Remove Old Log Files
sudo rm -f /var/log/gz

# Remove Thumbnail Cache
rm -rf ~/.cache/thumbnails/

# Remove old snaps
set -eu
snap list --all | awk '/disabled/{print $1, $3}' |
    while read snapname revision; do
        sudo snap remove "$snapname" --revision="$revision"
    done

-Aumentar swap.
-Activar Zswap, zram, zcache.
-Date
-Free
-Last reboot
-PWD
-Where is firefox, which firefox.
-ls -lh
-Chown
-Chattr
-Whoami
-Cat
-Dpkg
-Apt-get-check
-Dump
-gzip
-ifconfig
-hostname
-netstat
-uptime
-cat /etc/release
-neofetch
-hostnamectl
-uname -a
-cp -p
-https://github.com/Gregwar/notroot / Not root, install APT packages without root access
-https://github.com/guilhem/apt-get-install
-APt install que hace antes upgrade, clean, autoclean... https://github.com/guilhem/apt-get-install
cli-aptiX
Sinopsis
-Buscar paquetes por nombre y seleccionar desde lista cli https://github.com/BitJam/cli-aptiX
-Apt update, upgrade... A slim and simple tool to make updating and upgrading with apt-get faster and easier. This is a root-only tool since apt-get https://github.com/Yo-kai-Sei-shin-kage/aptupdater



